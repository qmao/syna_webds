import { LabIcon } from '@jupyterlab/ui-components';

import extensionConnectionSvg from '../style/icons/extensionConnection.svg';

export const extensionConnectionIcon = new LabIcon({
  name: 'webDS:extension-program',
  svgstr: extensionConnectionSvg
});
