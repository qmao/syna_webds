import { LabIcon } from '@jupyterlab/ui-components';
import LAUNCHER_ICON from '../style/icons/delta.svg';

export const launcherIcon = new LabIcon({
  name: 'jitsi:chat',
  svgstr: LAUNCHER_ICON
});